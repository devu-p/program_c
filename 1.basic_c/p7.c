//find area of rectangle formula:a=wl
 
#include<stdio.h>
void main()
{
    int w,l,area;
    printf("Enter the length of width:");
    scanf("%d",&w);
    printf("Enter the length of length:");
    scanf("%d",&l);
    area=w*l;
    printf("area of rectangle is: %d",area);
}
    